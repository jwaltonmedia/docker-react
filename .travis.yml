language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t jwaltonmedia/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true jwaltonmedia/docker-react npm run test
